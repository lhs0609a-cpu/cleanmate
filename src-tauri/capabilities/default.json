{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "클린PT 기본 권한. 최소 권한 원칙 — 필요한 것만 연다.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
