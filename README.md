# 클린메이트 (CleanMate) — 엔진 프로토타입

> 확실한 건 알아서, 애매한 것만 딱 물어보는 — 뭘 지우면 안 되는지 아는 정리 도구

기획: [마스터 기획서](https://claude.ai/code/artifact/4e328b41-b571-44f5-9915-61481e4a4692) ·
상세 문서는 바탕화면(`클린메이트_기획서_v2.txt`, `클린메이트_기능명세_v1.txt`, `클린메이트_고레버리지질문엔진_설계.txt`)

**웹 데모** → https://cleanmate-henna.vercel.app · 폴더를 고르면 브라우저 안에서 엔진이 돈다

## 지금 뭐가 되나

**Phase 0 — 엔진 검증 프로토타입.** 스캔 → 3-존 분류 → 미지수 클러스터링 → 고레버리지 질문 생성까지.
그리고 **숨은 공간 프로브**(`npm run probe`) — 스캐너가 구조적으로 못 보는 공간을 찾아
7문답으로 설명한다.

> ⚠️ **아무것도 삭제하지 않는다.** 분석과 질문 생성만 한다.
> 제1 개발 원칙: *"되돌릴 수 있는 그릇(격리·복구)이 준비되기 전엔 아무것도 지우지 않는다."*
> 격리 모듈(M11)이 붙기 전까지 삭제 코드는 존재하지 않는다.

## 개발 환경

**CLI는 의존성이 0이다.** Node 24 타입 스트리핑만 쓴다 — `npm install` 없이 바로 돈다.

```bash
npm run scan -- "C:/Users/you/Downloads"   # 설치 불필요
npm test                                    # 설치 불필요
```

웹 데모(`web/`)만 vite가 필요하다:

```bash
npm install && npm run dev
npm run build
```

> ⚠️ **Google Drive 폴더에서 `npm install` 하지 말 것.**
> 동기화 I/O 때문에 5분+ 걸리다 죽는다 (로컬 디스크는 **3초**). Drive에 node_modules를
> 두면 수천 개 파일을 동기화하려 들고 파일 잠금까지 걸어 삭제도 안 된다.
> **로컬 클론에서 작업한다** (`C:\dev\cleanmate`). GitHub이 진실의 원천이고,
> Drive 사본은 소스만 둔다. CLI는 의존성이 없어서 Drive에서도 그냥 돈다.

## 배포

GitHub → Vercel 자동 배포. `master`에 푸시하면 프로덕션에 나간다.

`vercel.json`의 **`connect-src 'none'`** — 데모가 사용자 파일을 읽으므로
브라우저가 네트워크를 강제 차단한다. *"파일은 기기를 떠나지 않습니다"* 를 말이 아니라
배포 설정으로 증명한다. 우리 코드가 데이터를 보내려 해도 브라우저가 막는다.

## 검증 결과 (실측)

핵심 가정 — *"존 B를 결정적 미지수로 묶으면 질문 3~4개로 대량을 해결할 수 있는가?"* → **참.**

| 대상 | 파일 | 용량 | 질문 | 질문당 해결 | 규칙 커버리지 |
|---|---|---|---|---|---|
| Downloads | 243 | 14.8GB | 4 | 3.71 GB/질문 | 86.8% |
| AppData/Local | 390,825 | 89.2GB | **3** | **8.94 GB/질문** | **99.6%** |

Downloads에서는 **질문 1개가 13.9GB(94%)를 해결**했다.

## 프로토타입이 잡아낸 실제 오탐

지식 DB가 없었으면 그대로 사고였을 것들. 전부 회귀 테스트로 잠갔다(`src/knowledge/paths.test.ts`).

1. **구글 드라이브 캐시 함정** — `AppData\Local\Google\DriveFS`의 메타데이터 DB 26.5GB를
   "평범한 큰 파일"로 보고 *"옮길까요?"* 라고 제안했다. 옮겼으면 드라이브 동기화가 통째로 깨진다.
   원인: 규칙이 `Google Drive`(공백)만 찾고 실제 경로 `Google\DriveFS`를 놓침.
   → 기획서 원본에 적힌 창업 인사이트를 프로토타입이 스스로 재현했다.
2. **WSL/Docker `.vhdx`** — 이동·삭제 대상으로 분류됐다. 실제로는 compact 전용.
3. **설치된 앱 안의 node_modules** — 사용자 프로젝트로 오인(635곳). 지우면 앱이 깨진다.
4. **Alfred 워크플로** — `workflows`를 경로 끝에서만 찾아 디렉토리 형태를 놓쳤다.
   CleanMyMac이 실제로 이걸 날린 사례가 있다.

수정 후: 존 C 잠금 648MB→**41.8GB**, 규칙 커버리지 86.8%→**99.6%**, 질문당 해결 3.71→**8.94GB**.
**안전과 효과가 함께 좋아졌다.**

## ★ 스캐너는 숨은 공간을 볼 수 없다 (실측)

```bash
npm run probe   # 숨은 공간 프로브 (읽기 전용)
```

개발 PC 실측: `readdir`는 `hiberfil.sys`·`pagefile.sys`·`swapfile.sys`를 전부 봤지만
`stat()`이 셋 다 **EPERM**으로 튕겼다. 시스템이 파일을 독점 잠금해서 열 수 없다.
그러면 `scanner.ts`가 이걸 `skipped`로 보내버려서 `files[]`에 못 들어오고,
분류도 엔진도 질문도 이 파일들을 **영원히 못 본다**.

| | |
|---|---|
| 이 PC의 hiberfil + pagefile | **40.7GB** |
| 우리 엔진에 보이는 양 | **0** |

기획서 17장이 *"파일트리 스캔이 못 보는 숨은 공간"* 이라고 쓴 게 우리 코드에서 그대로
재현됐다. **숨은 공간은 스캐너를 고쳐서 될 일이 아니라 별도 프로브로 가야 한다**는 게
실측으로 확정됐다.

부수 발견 — `paths.ts`의 `win.pagefile-hiberfil` 규칙은 셋을 한 덩어리로 잠근다.
그런데 셋은 `usedBy`가 전부 다르다(hiberfil=최대절전·빠른시작 / pagefile=가상메모리 /
swapfile=스토어앱). **설명을 쓰려고 하니 규칙이 뭉개져 있다는 게 드러났다** —
"설명이 곧 규칙 감사"가 첫날부터 작동했다.

### hiberfil — 설명 레이어의 첫 사례

리서치가 주장한 *"hiberfil은 RAM의 ~40%"* 를 실측으로 확인: RAM 31.7GB → **12.7GB (40.0%)**.
설명은 정적 문자열이 아니라 실측에서 생성된다 — RAM 크기, 노트북/데스크톱(섀시+배터리
독립 2신호), 빠른 시작 여부, 사용자 지정 크기에 따라 달라진다. *"당신 PC 얘기"* 여야
마음이 놓이지 일반론은 위키백과다.

지역화 함정도 잡았다: `powercfg /a` 출력은 한국어라 파싱하면 다른 언어 윈도우에서 깨진다.
→ 언어 무관 신호만 쓴다. **`hiberfil.sys`의 존재 자체가 최대절전이 켜졌다는 증거**다.

## 설계

```
probes/        숨은 공간. 스캔과 별개 경로 — 파일 트리에 안 보이기 때문에.
  facts.ts       시스템 사실 수집(판단 없음)
  hiberfil.ts    발견 + 7문답 설명 + 되돌릴 수 있는 회수 명령

scanner.ts     원시 사실만 수집. 판단 없음. (나중에 MFT로 교체)
  ↓
classify.ts    3-존 판정. 규칙 확증분만 존 A 자격 (R1 방어선)
  ↓
engine.ts      미지수 클러스터링 → 레버리지 점수 → 질문 3~4개
```

**knowledge/paths.ts 가 제품의 핵심 자산이다.** 두꺼울수록 콜드스타트(R2)가 해결되고
LLM 오분류(R1)에 덜 의존한다. 그리고 이건 **설명 자산이기도 하다** — 경쟁사는
사실 DB가 없어서 LLM을 붙이면 환각이 나온다. 해자가 두 겹이다.

### 설명 레이어 — "마음놓고 지우려면"

사람이 삭제 전에 실제로 묻는 건 7가지다. 한 줄짜리 `reason`으로는 1.5개밖에 못 답하고,
못 답하는 것들이 정확히 사람이 무서워하는 것들이다.

> ① 이게 뭐야 · ② 왜 이렇게 커 · **③ 뭐가 이걸 쓰고 있어** · ④⑤ 지우면 뭐가 달라져 ·
> ⑥ 되돌릴 수 있어 · ⑦ 안 지우면 어떻게 돼

- **사실은 DB, 표현만 LLM.** 설명은 사용자가 검색해서 검증할 수 있다. 한 번 지어내면
  신뢰가 끝난다. `engine.ts`의 질문 선정과 같은 원칙 — LLM에 판단 권한을 주지 않는다
- **★ `usedBy`를 못 쓰는 규칙은 SAFE 자격이 없다.** "뭐가 이걸 쓰는지" 모른다 =
  "뭐가 깨지는지" 모른다는 뜻이다. Alfred 워크플로 참사도 DriveFS 26.5GB 오탐도
  전부 이걸 몰라서 났다. **설명을 강제하면 엉터리 SAFE 규칙이 구조적으로 못 들어온다**
- **양면 정직** — 손해를 반드시 쓴다(hiberfil은 "부팅 2~5초 느려짐"). 좋은 점만 쓰면 광고다
- **`ifKept` 필수** — "안 지워도 된다"는 선택지를 절대 뺏지 않는다. 없으면 압박이 된다

### 안전 불변식

- 모르는 파일은 **절대 SAFE로 추측하지 않는다** → AMBIG(물어봄). 추론은 자동 처리 자격 없음
- 질문의 답은 **재분류만** 한다. 어떤 답도 즉시 삭제를 유발하지 않는다
- '아니오/모르겠음'은 **항상 보존 쪽으로** 폴백
- 존 C는 스캔조차 하지 않는다(`SKIP_DIRS`) — 실수의 여지를 원천 차단

## 다음

**삭제 코드 없이 출시 가능한 데까지 먼저 간다.** 숨은 공간은 파일 삭제가 아니라 공식
명령이고 되돌리기가 한 줄이라, 격리(M11) 없이도 안전하다. 그동안 *"안 지우는 것"* 이 기능이다.

- [x] hiberfil 프로브 + 설명 레이어 — **12.7GB** (이 PC)
- [ ] **실행 경로** — 확인 흐름 + 관리자 권한 + 되돌리기 버튼 + 감사 로그
- [ ] **답변 루프** — "최대절전 쓰세요?" → 답 → 반영. 지금 `outcome` 타입만 있고
      소비하는 코드가 0이라 **★핵심 가정(대화형 UX)을 측정할 방법이 없다**
- [ ] **지킨 용량 UI** — 숫자는 이미 있다(`cli.ts:79`, 41.8GB). 경쟁사가 못 하는 말
- [ ] 나머지 팽창 범인 — pagefile(28GB, 이동/축소만) · SoftwareDistribution ·
      WinSxS(DISM) · Windows.old · vhdx compact
- [ ] **M11 격리·복구** — 사용자 *파일*을 지우기 시작할 때. 그 전엔 필요 없다
- [ ] 사용자 데이터 경로 지식 — 사진·영상·문서. **지금 jpg/png 규칙이 0개**라
      사진은 전부 '알 수 없는 파일'로 떨어진다. 정작 사용자 용량의 최대 축인데
- [ ] MFT 직접 읽기 (현재 390k 파일 33초 → 수초 목표. Rust)
- [ ] 온디바이스 LLM — AMBIG의 미지수 추정 + 설명 '표현'만.
      **SAFE 승격 권한도, 사실을 지어낼 권한도 주지 않는다**
- [ ] ★ R1 실측: 존 A 오분류율. 오분류 시 격리가 손실을 100% 막는가
- [ ] ★ 미검증 가정: 질문형 UX가 단순 리스트보다 나은가 → A/B

## 스택

Node 24 (타입 스트리핑, 빌드 없음). 최종은 Rust 코어 + Tauri.
엔진 로직은 스캐너 교체와 무관하게 유지된다.
